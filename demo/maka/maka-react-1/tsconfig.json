{
    
    "compilerOptions": {
        "target": "ES5",
        "experimentalDecorators": true,
        "resolveJsonModule": true,
        "lib":[
            "es6",
            "DOM"
        ],
        //检测导入的模块是否是 ES6 模块，如果不是，则查找模块中是否有 exports.default 导出
        "allowSyntheticDefaultImports": true,
        // `preserve`的配置让`tsc`不会去处理它，而是使用后续的`babel-loader`进行处理
        "jsx":"preserve"
    },
    "exclude": [
        "node_modules"
    ]
}